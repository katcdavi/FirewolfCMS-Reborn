<!DOCTYPE html>
<html lang="en">
    <head>
        <title><?= $parent->getTitle() ?></title>

        <link rel="stylesheet" href="./style/default.css">
    </head>

    <body>
        <h1>
            <?= $controller->getTitle() ?>
        </h1>

        <?php
            if ($controller !== null) {
                $controller->render();
            }
        ?>
    </body>
</html>