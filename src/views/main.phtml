<!DOCTYPE html>
<html lang="en">
    <head>
        <title><?= $parent->getTitle() ?></title>
    </head>

    <body>
        <h1>
            <?= $controller->getTitle() ?>
        </h1>

        <?php
            if ($controller !== null) {
                $controller->render();
            }
        ?>
    </body>
</html>